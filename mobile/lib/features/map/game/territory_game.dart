import 'package:flame/game.dart';

class TerritoryGame extends FlameGame {
  @override
  Future<void> onLoad() async {
    await super.onLoad();
    // TODO: Add live map overlays, territory polygons, and capture effects.
  }
}
